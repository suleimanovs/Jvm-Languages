package enums;

/**
 * Enum - класс перечислений, класс содержащий некий набор констант которые обединяются неким типом.
 * Этот некий тип и есть имя Enum class
 */
public enum Color {
    BLUE,
    RED,
    BLACK,
    WHITE,
    GREEN,
    PURPLE;
}
